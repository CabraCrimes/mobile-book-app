
class Book {
    constructor(id, title, author, rating, genre, summary ) {
        (this.id = id),
        (this.title = title),
        (this.author = author),
        (this.rating = rating),
        (this.genre = genre),
        (this.summary = summary);
    }
  }
  
module.exports = Book;